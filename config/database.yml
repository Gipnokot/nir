default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development: &development
  adapter: postgresql
  host: localhost
  port: 5432
  database: nirdb
  username: postgres
  password: postgres

test:
  <<: *default
  database: nir_test

production:
  <<: *development
